# Sanctum — Effort 2: [REDACTED] (future stub)

*Sanctum · S2 effort · not built. Placeholder for the second intelligence effort under the Sanctum umbrella.*

**BLUF:** Reserved space. Nothing here is built yet. This effort stands up only after the CTI effort is keeper-proven (see `../ROADMAP.md`).

## What this will be

A weekly intelligence cycle supporting a [REDACTED]. It shares Sanctum's machinery — collection patterns, scoring discipline, the human-gate philosophy — but keeps its own doctrine.

- **Frameworks (its own doctrine):** IPB — MCOO, OAKOC, METT-TC, ASCOPE, PMESII-PT.
- **Classification:** all work **unclassified / open-source**, same as the CTI effort.
- **Reuse:** the intelligence cycle is domain-neutral; only the requirements (IRs) and sensors swap. The shared engine is extracted from the CTI effort only *after* a second real use case exists (see the agnostic-vision section of `../ROADMAP.md` — "don't abstract before the second real use case exists").

## Status

Not started. The agnostic engine already supports this domain — `pnd.md` here is an empty P&D template (manifest + IPB-flavored scoring skeleton + production). Fill it in and add `config/sensors.txt`, then `../run.sh s2` runs the same collect→score machinery. Do not build until: (1) this S2 role is a real, active requirement, and (2) ideally the CTI effort has proven itself first.
