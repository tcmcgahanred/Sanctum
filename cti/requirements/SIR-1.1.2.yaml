# ------------------------------------------------------------------------------
#  SIR-1.1.2 · An extortion or leak-site post naming a California victim
#
#  One requirement, one file. Copy the closest existing rule rather than
#  starting from a skeleton; the one you copy is live and tested.
#
#  status   stable      finished, and something collects for it
#           unsupported finished, but its `logsource:` matches no sensor in
#                       the manifest, so nothing can ever reach it
#           draft       the wording is proposed, not settled
#           blocked     a machine could decide it, but the input it needs does
#                       not exist yet. `blocked_by:` names that input.
#
#  When more than one fits, the precedence is blocked, then unsupported, then
#  draft, then stable.
#
#  EVERY requirement is machine decidable. There is no value that parks one
#  permanently. A requirement with no `detection:` block is either a detector
#  nobody has written or a named missing input, and both are meant to be
#  worked off rather than excused.
#
#  `falsepositives:` records what WILL match and should not. Write it here,
#  beside the rule, not in a changelog nobody reads beside the rule.
# ------------------------------------------------------------------------------
id: SIR-1.1.2
title: An extortion or leak-site post naming a California victim
status: unsupported
modified: '2026-09-24'
fact: Extortion or leak-site posts naming California victims.
logsource:
  scope: national
  kind: leak_site
sensor:
  name: ransomware leak-site aggregator, CA-filtered
  status: PENDING
detection:
  california:
    keywords:
    - california
    - californian
    - ' calif '
    - sacramento
    - fresno
    - modesto
    - stockton
    - bakersfield
    - cal oes
    - cal-csic
    - ccic
    - caltrans
    - 'csu '
    - 'uc '
    - calmatters
    - alpine county
    - amador county
    - butte county
    - calaveras county
    - colusa county
    - el dorado county
    - fresno county
    - glenn county
    - inyo county
    - kern county
    - kings county
    - lake county
    - lassen county
    - madera county
    - mariposa county
    - mendocino county
    - merced county
    - modoc county
    - mono county
    - nevada county
    - placer county
    - plumas county
    - sacramento county
    - san joaquin county
    - shasta county
    - sierra county
    - stanislaus county
    - sutter county
    - tehama county
    - trinity county
    - tulare county
    - tuolumne county
    - yolo county
    - yuba county
    scope: blob
  leak_site:
    keywords:
    - leak site
    - leak-site
    - data leak site
    - extortion site
    - victim list
    - listed the
    - posted the victim
    - named the victim
    - claimed responsibility
    - ransom note
    - double extortion
    - publish the stolen
    - threatened to publish
    scope: blob
  ransom:
    keywords:
    - ransomware
    - ransom
    - extortion
    - encrypt
    - leak site
    - double extortion
    scope: blob
  condition: california and (leak_site or ransom)
note: WRITTEN for this requirement and NOT yet measured against the corpus. Run tools/requirement_coverage.py
  on Ravenor to see what it matches before trusting the count.
